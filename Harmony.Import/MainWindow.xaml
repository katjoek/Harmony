<Window x:Class="Harmony.Import.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Harmony.Import"
        mc:Ignorable="d"
        Title="Harmony Data Import" Height="600" Width="900"
        MinHeight="400" MinWidth="600">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Sheet 1 File Selection -->
        <Grid Grid.Row="0" Margin="0,0,0,10">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <Label Grid.Column="0" Content="Sheet 1 (Personen):" VerticalAlignment="Center" Margin="0,0,10,0"/>
            <TextBox Grid.Column="1" Text="{Binding Sheet1FilePath, UpdateSourceTrigger=PropertyChanged}" 
                     VerticalAlignment="Center" Margin="0,0,10,0" IsReadOnly="True"/>
            <Button Grid.Column="2" Content="Browse..." Command="{Binding BrowseSheet1Command}" 
                    Padding="10,5" MinWidth="80"/>
        </Grid>

        <!-- Sheet 2 File Selection -->
        <Grid Grid.Row="1" Margin="0,0,0,10">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <Label Grid.Column="0" Content="Sheet 2 (Groepen):" VerticalAlignment="Center" Margin="0,0,10,0"/>
            <TextBox Grid.Column="1" Text="{Binding Sheet2FilePath, UpdateSourceTrigger=PropertyChanged}" 
                     VerticalAlignment="Center" Margin="0,0,10,0" IsReadOnly="True"/>
            <Button Grid.Column="2" Content="Browse..." Command="{Binding BrowseSheet2Command}" 
                    Padding="10,5" MinWidth="80"/>
        </Grid>

        <!-- Start Import Button -->
        <Button Grid.Row="2" Content="Start Import" Command="{Binding StartImportCommand}" 
                Padding="20,10" FontSize="14" FontWeight="Bold" Margin="0,0,0,10"
                IsEnabled="{Binding CanStartImport}"/>

        <!-- Log Display -->
        <GroupBox Grid.Row="3" Header="Import Log" Margin="0,0,0,10">
            <ScrollViewer x:Name="LogScrollViewer" VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Auto">
                <TextBox x:Name="LogTextBox" Text="{Binding LogText, Mode=OneWay}" 
                         IsReadOnly="True" 
                         TextWrapping="Wrap"
                         VerticalAlignment="Stretch"
                         HorizontalAlignment="Stretch"
                         FontFamily="Consolas"
                         FontSize="11"
                         Background="#F5F5F5"
                         BorderThickness="0"
                         TextChanged="LogTextBox_TextChanged"/>
            </ScrollViewer>
        </GroupBox>

        <!-- Status Bar -->
        <StatusBar Grid.Row="4">
            <StatusBarItem>
                <TextBlock Text="{Binding StatusText}"/>
            </StatusBarItem>
        </StatusBar>
    </Grid>
</Window>
